package com.joeblakeb.battleships.views

/**
 * The interface with required methods for both gameplay views to work with turns.
 */
interface GameplayGameBoardView {
    /** It is now this players turn */
    fun haveTurn()
}